<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="prose mx-auto max-w-md">
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <div>
      <% if user_signed_in? %>
        <%= link_to "New blog post", new_blog_post_path, class: "m-2 bg-sky-200 px-4 py-2 rounded hover:bg-sky-300" %>
        <%# <%= link_to "Profile", edit_user_registration_path %>
        <%= button_to "Log Out", destroy_user_session_path, method: :delete, class: "m-2 bg-gray-100 px-4 py-2 rounded hover:bg-gray-200" %>
      <%# Be sure to put back :registrable in user controller before uncommenting below %>
      <%# <% else %>
        <%# <%= link_to "Log In", new_user_session_path %>
        <%# <%= link_to "Sign Up", new_user_registration_path %>
      <% end %>
    </div>

    <%= yield %>
  </body>
</html>
